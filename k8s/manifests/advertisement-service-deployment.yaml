apiVersion: apps/v1
kind: Deployment
metadata:
  name: advertisement-service
  namespace: multi-service-platform
  labels:
    app: advertisement-service
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: advertisement-service
  template:
    metadata:
      labels:
        app: advertisement-service
        version: v1
    spec:
      containers:
      - name: advertisement-service
        image: multi-service-platform/advertisement-service:latest
        ports:
        - containerPort: 3006
        env:
        - name: PORT
          value: "3006"
        - name: DATABASE_URL
          value: "postgresql://$(POSTGRES_USER):$(POSTGRES_PASSWORD)@$(POSTGRES_HOST):$(POSTGRES_PORT)/ads_db"
        envFrom:
        - configMapRef:
            name: platform-config
        - secretRef:
            name: platform-secrets
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3006
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 3006
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: advertisement-service
  namespace: multi-service-platform
  labels:
    app: advertisement-service
spec:
  selector:
    app: advertisement-service
  ports:
  - port: 3006
    targetPort: 3006
    protocol: TCP
  type: ClusterIP