apiVersion: apps/v1
kind: Deployment
metadata:
  name: file-service
  namespace: multi-service-platform
  labels:
    app: file-service
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: file-service
  template:
    metadata:
      labels:
        app: file-service
        version: v1
    spec:
      containers:
      - name: file-service
        image: multi-service-platform/file-service:latest
        ports:
        - containerPort: 3008
        env:
        - name: PORT
          value: "3008"
        - name: DATABASE_URL
          value: "postgresql://$(POSTGRES_USER):$(POSTGRES_PASSWORD)@$(POSTGRES_HOST):$(POSTGRES_PORT)/file_db"
        envFrom:
        - configMapRef:
            name: platform-config
        - secretRef:
            name: platform-secrets
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3008
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 3008
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: file-service
  namespace: multi-service-platform
  labels:
    app: file-service
spec:
  selector:
    app: file-service
  ports:
  - port: 3008
    targetPort: 3008
    protocol: TCP
  type: ClusterIP