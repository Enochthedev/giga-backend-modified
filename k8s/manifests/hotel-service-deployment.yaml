apiVersion: apps/v1
kind: Deployment
metadata:
  name: hotel-service
  namespace: multi-service-platform
  labels:
    app: hotel-service
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: hotel-service
  template:
    metadata:
      labels:
        app: hotel-service
        version: v1
    spec:
      containers:
      - name: hotel-service
        image: multi-service-platform/hotel-service:latest
        ports:
        - containerPort: 3005
        env:
        - name: PORT
          value: "3005"
        - name: DATABASE_URL
          value: "postgresql://$(POSTGRES_USER):$(POSTGRES_PASSWORD)@$(POSTGRES_HOST):$(POSTGRES_PORT)/hotel_db"
        envFrom:
        - configMapRef:
            name: platform-config
        - secretRef:
            name: platform-secrets
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3005
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 3005
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: hotel-service
  namespace: multi-service-platform
  labels:
    app: hotel-service
spec:
  selector:
    app: hotel-service
  ports:
  - port: 3005
    targetPort: 3005
    protocol: TCP
  type: ClusterIP